import sympy as sp
from sympy import symbols

x, y, z = symbols('x y z')
func = x**2+y**2+z**2
def eval_function(f, point):
    result = f.evalf(subs={x: point[0], y: point[1]})
    return result
print(eval_function(func, (1, 2)))